package com.pop.insta.ui

import androidx.compose.ui.graphics.Color

/** A dark workbench, so the POP on the table is the only bright thing. */
object Skin {
    val Shell = Color(0xFF121214)
    val Panel = Color(0xFF1C1C21)
    val PanelHi = Color(0xFF26262D)
    val Line = Color(0xFF34343C)

    val Text = Color(0xFFF4F2EC)
    val TextDim = Color(0xFF8E8E98)

    /** The one accent, carried over from the launcher in this repo. */
    val Accent = Color(0xFFE9653C)
    val Marker = Color(0xFFF2C50E)
}
